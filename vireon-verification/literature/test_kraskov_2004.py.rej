diff a/vireon-verification/literature/test_kraskov_2004.py b/vireon-verification/literature/test_kraskov_2004.py	(rejected hunks)
@@ -13,10 +13,11 @@ def test_kraskov_mutual_information_functional_dependence():
     t = np.linspace(0, 4 * np.pi, 500)
     x = np.sin(t)
     y = np.cos(t)  # Non-linear functional relationship (circle in 2D space)
-    
-    mi = VireonMutualInformation(n_bins=10)
+
+    # k=4 (KSG estimator parameter) — replaced the legacy n_bins=10 arg.
+    mi = VireonMutualInformation(k=4)
     score = mi.compute(x, y)
-    
+
     assert score > 0.50, f"Expected MI > 0.50 for functional relationship, got {score:.4f}"
 
 
@@ -25,8 +26,8 @@ def test_kraskov_mutual_information_uncorrelated_noise():
     np.random.seed(2004)
     x = np.random.randn(500)
     y = np.random.randn(500)
-    
-    mi = VireonMutualInformation(n_bins=10)
+
+    mi = VireonMutualInformation(k=4)
     score = mi.compute(x, y)
-    
+
     assert score < 0.15, f"Expected MI < 0.15 for independent noise, got {score:.4f}"
