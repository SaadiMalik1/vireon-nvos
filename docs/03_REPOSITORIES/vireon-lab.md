# vireon-lab

`vireon-lab` is the sandbox repository. 

It contains interactive Jupyter Notebooks, Streamlit dashboards, and tutorial scripts designed to help researchers visualize the effects of the `vireon-models` digital twins (e.g., dynamically dragging a slider to see how increasing skin impedance degrades the spatial resolution of a CSP filter in real-time).