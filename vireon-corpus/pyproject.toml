[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "vireon-corpus"
version = "0.1.0"
description = "VIREON Scientific Corpus and Dataset Plugins"
dependencies = [
    "vireon-core",
]
