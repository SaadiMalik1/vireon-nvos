# VIREON API configuration example
VIREON_API_KEY=  # Set to a random secret string in production to enable auth
VIREON_CORS_ORIGINS=http://localhost:3000,http://localhost:8000
VIREON_DB_PATH=evidence_registry.db
